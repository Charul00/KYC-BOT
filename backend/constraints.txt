# Constraints file for Render deployment
# Force pre-built wheels to avoid Rust compilation errors
# Render environment has read-only filesystem restrictions

# Maturin-based packages (must use wheels)
tokenizers==0.15.1
cryptography>=41.0.0

# Python 3.12 compatibility required
# Pins versions that have wheels for Python 3.12+
